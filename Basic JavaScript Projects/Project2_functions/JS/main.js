function test_1(str1) {  //This defines the function and parameters
    str1 = "This " //This sets a value to the variable
    str1 += "is easy!" //Concatenates the strings
    document.getElementById("new").innerHTML = str1; //This changes the HTML text
}

function easy() {
    document.getElementById("ano").innerHTML = "Now you see it!";
}